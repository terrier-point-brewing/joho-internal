---
name: project_financials_consolidation
description: "Finance Model/Sales/Statements → unified Financials view; FULLY BUILT + cut over + parity-green on branch, PR-ready"
metadata: 
  node_type: memory
  type: project
  originSessionId: 27ed032d-0ea1-48f4-9045-4950522ae993
---

2026-07-12: Consolidated Finance **Model + Sales + Statements** into ONE **Financials** subtab on a **single chart-of-accounts-mapped basis** (channel/category/BBL-volume are orthogonal slices of the same mapped rows → reconciliation-by-construction). Spec `docs/superpowers/specs/2026-07-11-financials-consolidation-design.md`; plan `docs/superpowers/plans/2026-07-11-financials-consolidation.md`. Branch `claude/eloquent-jang-29446f`, HEAD `51fff28`, 26 commits ahead of origin/main, working tree clean, **1010/1010 tests + `npm run build` + search-filter guard all green. MERGED to main via squash PR #174 (commit 2110c44).** SDD ledger: `.superpowers/sdd/progress.md`.

**COMPLETE — all 15 plan tasks + fixes + cutover:** `lib/finance/financials/` pipeline (`normalizeSign`→`dimensions`→`volume`→`aggregateRows`→`summaries`→`buildFinancials`, fetch in `fetchSources.ts`) → admin-gated `GET /api/finance/financials` → page `app/finance/financials/` (`buildTree`+`FinancialsTable` single renderer, KPI strip, $/BBL toggle, search/filter/sort via shared standard, `DataQualityPanel`). **Cut over (Tasks 13/14):** nav = [Financials, Transactions, Payroll, Settings]; old Model/Sales/Statements pages redirect to `/finance/financials` (statements preserve `?statement=pl|balance_sheet|cash_flow`); old sales/statements API routes + SalesTable/SalesPageShell/Nav/UnrecognizedBanner/statements-lib + orphaned `app/finance/hooks/queries.ts` deleted. PROTECTED (new pipeline deps, kept): `lib/finance/invoiceSalesReport.ts`, `lib/reports/{taproom-model,kegs,bbl-tracker}.ts`, `lib/finance/accountSections.ts`.

**Bugs caught & fixed during review/parity (the process worked):** Final Opus review found 3 money bugs → C1 `normalizeSign` `Math.abs()` 2×-overstated expenses on signed-source credits (fixed: pass-through `rawCents` for expense/bank on P&L sections); C2 `fetchBank` ignored `affects_pl` → settlement double-count (fixed: filter `affects_pl=true` for pl/cash_flow); I1 Gross Profit included other_income vs KPI (fixed: revenue−COGS only). M1 unrecognized-`statement_section` rows silently dropped (fixed: "Other" catch-all in `buildTree`, included in Net Income). M2 draft pours flooded unknownVolume bucket (fixed: shared `isUnknownVolume` excludes DRAFT_BEER). C3 BS open-invoice A/R ported (`fetchOpenInvoiceAr`+`injectOpenInvoiceAr`, mirrors old statements route:474-488). **Parity harness (`scripts/financials-parity.ts`) caught a CRITICAL bug no unit test could: all `fetchSources` queries were UNPAGINATED → silent PostgREST 1000-row cap dropped 1724 of 2724 pos rows (all of May). Fixed with `fetchAllRows` pagination + stable order.**

**PARITY GREEN (Supabase slice, run live vs independent SQL ground-truth):** Revenue/COGS/Expenses/Net Income **$0.00 delta** for 2026-05/06/07; reconciliation invariant PASS all months; BS A/R $9,383.76 exact match. buildFinancials reads Supabase only (no live Square at runtime).

**DEFERRED (need working LOCAL Square token; NOT code blockers — verify pre/post-merge):** (1) Square-dependent parity slice = new channel numbers vs old Sales/Taproom tabs; (2) browser visual verification (finance is admin-gated, dev env has no admin session). Local `.env.local` Square token is NOW WORKING (64-char, prefix 'EAAA', auth 200 as of 2026-07-12) — the earlier malformed 65-char 'EEAA' token has been replaced. See [[project_square_token_rotation]].

**2026-07-12 — June POS sync gap RESOLVED (was the top OPEN follow-up).** `scripts/financials-parity-square.ts` had flagged 2026-06 as missing ~50% of live-Square POS orders (371 persisted vs 744 live; May 446/446 & July fine). **Root cause (NOT a duplicate/race — different from [[project_taproom_sync_race]] which produced dup rows): the near-real-time Square webhook stopped landing orders from 2026-06-14 onward (June 1–13 fully present, 14+ almost entirely missing bar a few later-resynced stragglers), AND the `finance-sync` safety-net cron did not exist during June — earliest `cron_runs` entry is 2026-07-08. That cron's 3-day trailing window (`WINDOW_DAYS=3` in `app/api/cron/finance-sync/route.ts`) never reaches back to mid-June, so those days were permanently stranded until a manual full-month re-sync.** Fix: `scripts/backfill-june-2026-pos.ts` (idempotent, bypasses `unstable_cache` fetchers via raw `squarePostAll`/`squareGetAll` + `syncSquareOrders`/`syncRefunds` cores) re-synced all June → persisted now 759 vs 744 live = **-2.0% gap (same closed_at-vs-created_at + retained-CANCELED-row artifact July shows)**, down from -50%. Auto-map left only 2 unmapped June line items (variations with no mapping rule — expected residual). Diagnostic: `scripts/diagnose-june-sync-gap.ts`. **Go-forward hardening NOT yet done (recommended): widen the finance-sync cron window or add a monthly full-month re-sync, so a future webhook outage self-heals instead of stranding once days age past 3 days.** The exact reason the webhook broke on 6/14 is the one open unknown (needs Square dashboard delivery logs).

**Fixed 2 latent bugs in `lib/finance/autoMap.ts` `autoMapPosLineItems` (blocked all POS auto-mapping app-wide — the `POST /api/finance/transactions/auto-map` route + the rule-edit auto-map trigger):** (1) filtered `pos_line_items.square_order_id` which doesn't exist — the FK column is `order_id`; (2) unbounded `.in("order_id", <all ~1613 year order UUIDs>)` overflowed PostgREST URL length → 400 — now chunked at 200. Pure resolver `resolvePosBackfill` was already tested & correct; only the IO wrapper (not unit-testable without a live PostgREST mock) was broken, which is why the parity/backfill integration scripts caught it. All 1294 tests + tsc green post-fix.
