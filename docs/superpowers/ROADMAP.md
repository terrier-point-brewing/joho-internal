# Invoicing / Kegging-Canning / Cold Storage / Export — Roadmap & Orchestration

This file is the single source of truth for a multi-spec feature build spanning several sessions. If you are a fresh session picking this up, read this file fully before doing anything else — it replaces the conversation history that produced it.

## Canonical spec list

Numbering is authoritative as of the most recently written spec (`2026-06-21-allocation-adjustment-refund-design.md`). Earlier spec docs have slightly different numbering in their own headers — trust this list, not them, if they ever disagree.

| # | Spec | Status | Spec doc | Plan doc | PR |
|---|------|--------|----------|----------|-----|
| 1 | Cold Storage + Transfer Log schema | **Merged** | `docs/superpowers/specs/2026-06-20-cold-storage-transfer-log-design.md` | `docs/superpowers/plans/2026-06-20-cold-storage-transfer-log.md` | [#17](https://github.com/terrier-point-brewing/terrier-point-brewing/pull/17) |
| 2a | Export Transaction model + batch-completion automation | **Merged** | `docs/superpowers/specs/2026-06-20-export-transactions-design.md` | `docs/superpowers/plans/2026-06-20-export-transactions.md` | [#18](https://github.com/terrier-point-brewing/terrier-point-brewing/pull/18) |
| 2b | Export Bay UI | **Merged** | `docs/superpowers/specs/2026-06-21-export-bay-design.md` | `docs/superpowers/plans/2026-06-21-export-bay.md` | [#19](https://github.com/terrier-point-brewing/terrier-point-brewing/pull/19) |
| 2c-1 | Ad-Hoc Export | **Merged** | `docs/superpowers/specs/2026-06-21-ad-hoc-export-design.md` | `docs/superpowers/plans/2026-06-21-ad-hoc-export.md` (**uncommitted as of this writing — safe, unrelated to 2c-2, commit whenever convenient**) | [#20](https://github.com/terrier-point-brewing/terrier-point-brewing/pull/20) |
| 2c-2 | Allocation adjustment + refund flow | **Merged** | `docs/superpowers/specs/2026-06-21-allocation-adjustment-refund-design.md` | `docs/superpowers/plans/2026-06-21-allocation-adjustment-refund.md` | [#21](https://github.com/terrier-point-brewing/terrier-point-brewing/pull/21) |
| 7-pre | Fix `lib/auth.ts` false role hierarchy (manager/brewer are scoped siblings, not ranked) | **Merged** | `docs/superpowers/specs/2026-06-21-auth-role-hierarchy-fix-design.md` | `docs/superpowers/plans/2026-06-21-auth-role-hierarchy-fix.md` | [#22](https://github.com/terrier-point-brewing/terrier-point-brewing/pull/22) |
| 7 | Export Settings + Barrel Excise Tax settings (mirrored to Finance > Settings) | **Merged** | `docs/superpowers/specs/2026-06-21-export-settings-design.md` | `docs/superpowers/plans/2026-06-21-export-settings.md` | [#23](https://github.com/terrier-point-brewing/terrier-point-brewing/pull/23) |
| 6 | Export > Commitments unified invoicing (Packaging Fees / Bulk Discount / Excise / Keg Cleaning / Forklift) | **Merged** | `docs/superpowers/specs/2026-06-22-export-unified-invoicing-design.md` | `docs/superpowers/plans/2026-06-22-export-unified-invoicing.md` | [#24](https://github.com/terrier-point-brewing/terrier-point-brewing/pull/24) |
| 9 | Packaging Variations foundation — new `packaging_variations` table (container + format + lid/paktech/tray/label component slots + optional partner) and `recipe_packaging_variations` join table, plus a definition UI and a Recipes UI section. Makes a packaging variation definable as a first-class reusable entity; consumes nothing downstream yet. | **Merged** | `docs/superpowers/specs/2026-06-23-packaging-variations-foundation-design.md` | `docs/superpowers/plans/2026-06-23-packaging-variations-foundation.md` | [#25](https://github.com/terrier-point-brewing/terrier-point-brewing/pull/25) |
| 10 | Brewing/kegging-canning + cold storage **strict** wiring — `TransferModal.tsx` changes from ad-hoc component assembly + free-text `variant_label` to picking strictly from that recipe's `recipe_packaging_variations`-declared set; `transfers/route.ts` and `cold_storage_inventory` key off `variation_id` only (no free text, no fallback to old shape); fix the demand-calendar proxy-lookup bug (`demand-calendar/route.ts:73-81`) by joining through the variation model. **No backfill of existing free-text `variant_label` rows** — user-confirmed reset, fresh transfers go through the new model going forward. | **Merged** — `batch_transfers`/`cold_storage_inventory` rekeyed to `variation_id`; new `packaging_variations.total_volume_fl_oz` (trigger-maintained) is the single source of truth for volume math; `ExportBayTab.tsx`/export-bay ship routes also rekeyed (forced by the cold-storage rekey, resolved as in-scope per this session's brainstorm rather than deferred); demand-calendar bug fixed via real join. Mid-implementation scope additions: 5 more `kegging_detail`/`canning_detail` consumers found in `BrewStatusTab.tsx`/`BrewsSubtab.tsx` (not in the original plan, absorbed into Task 5); a pre-existing Spec 9 seed-data bug (6 `packaging_variations` rows missing required `tray_id`/`paktech_id`, plus a missing 6-count paktech catalog item) found and fixed directly against the live DB during Task 1. | `docs/superpowers/specs/2026-06-23-brewing-cold-storage-strict-wiring-design.md` | `docs/superpowers/plans/2026-06-23-brewing-cold-storage-strict-wiring.md` | [#27](https://github.com/terrier-point-brewing/terrier-point-brewing/pull/27) |
| 11 | Export wiring — Export Settings' Packaging Fee mapping moves from `packaging_item_id` to mapping over **`packaging_items` rows filtered to `type in ('keg','can')` directly** (the 5 canonical containers, regardless of whether any `packaging_variations` row exists for them yet) — **not** `packaging_variations.container_id`, since that would only surface containers that already have a defined variation and reintroduce the exact fragmentation Spec 11 exists to fix (e.g. a container nobody's made a variation for yet still needs a fee settable). Fixes the `ExportSettingsPanel.tsx:248` fragmentation bug properly (mapping over containers only, by `type`, mirroring the precedent in `PackagingVariationsPanel.tsx:39`'s `packaging.filter(p => p.type === "keg" \|\| p.type === "can")`) instead of a type-filter patch. Not directly affected by the strict-consumption rework — that principle governs recipe→variation selection downstream (transfers/cold storage/commitments), not Settings-level fee config, which should exist per container independent of variation existence. Confirm during brainstorming whether `ExportBayTab.tsx`'s ship/inventory-line picker also needs to move off free-text `variant_label` once Spec 10 lands, or whether that's already covered by Spec 10's cold-storage rekey. | **Merged** — investigation found `export_transactions.packaging_item_id` already only ever holds a container id in practice, so no schema/rename needed; fix was UI-filter (Task 1) + fail-loudly invoicing validation (Task 2) only | `docs/superpowers/specs/2026-06-23-export-wiring-design.md` | `docs/superpowers/plans/2026-06-23-export-wiring.md` | [#26](https://github.com/terrier-point-brewing/terrier-point-brewing/pull/26) |
| 8 | Deposit invoice flow ↔ Export invoice flow parity, now also covering Intake/Commitments packaging-preference + deposit-invoice cost calculation switching to **strict** variation-based component costs — `commitment_packaging_preferences` should pick only from the recipe's `recipe_packaging_variations`-declared set (same strict-consumption principle as Spec 10), not freely choose any container/component combo | **Merged** — module merge (`lib/square/square-invoices.ts`), `invoice_item_mappings` rename + `ingredient_deposit` type, deposit/export invoice `generate\|send\|sync` parity (closing the export-never-reaches-paid gap), new Production Settings nav, `ExportSettingsPanel.tsx` bug-fix bundle, and `commitment_packaging_preferences` strict rekey (UI/API only — packaging cost on deposit invoices explicitly deferred to a future spec, per user decision mid-brainstorm) | `docs/superpowers/specs/2026-06-26-deposit-export-invoice-parity-design.md` | `docs/superpowers/plans/2026-06-26-deposit-export-invoice-parity.md` | [#28](https://github.com/terrier-point-brewing/terrier-point-brewing/pull/28) |

## ⚠️ Active situation as of this handoff

Spec 2c-2 is confirmed merged (PR #21, merge commit `8bd7481`, merged 2026-06-22). Local `main` was reset (`git reset --hard origin/main`, user-confirmed) to clear 4 stale local-only commits from that session's earlier shared-checkout mistake; local now matches `origin/main` exactly.

**Specs 6 and 7 were reordered.** Brainstorming for Spec 6 got partway through clarifying questions before revealing that "unified invoicing" needs a Customer × Service → Square Item mapping plus configurable Barrel Excise Tax rates (receiving party, unit, rate) — neither exists yet, and both are exactly Spec 7's scope. Spec 7 is a hard prerequisite for Spec 6, not an independent/parallel item. Decision: do Spec 7 first (full brainstorm → spec → plan → implement → merge cycle), then resume Spec 6 in a separate future session.

**Spec 6 design progress to resume from** (captured during the aborted brainstorm, not yet written to a spec doc): the final invoice will be tied to **Export Transactions**, not Commitments directly. Export Transactions get a new status `Invoice Required` on creation (regardless of whether the Ship action came from a Commitment-linked or ad-hoc export). A new unified Export > Export Transactions view (replacing/merging the current Distribution + Contract Brewing subtabs) groups transactions by Customer. The user multi-selects same-customer `Invoice Required` transactions and generates one combined Square invoice with auto-populated line items (editable/addable/removable before finalizing); on creation, those Export Transactions move to status `Unpaid`. Whoever resumes Spec 6 should re-confirm these decisions with the user rather than assuming they still hold, then continue the brainstorming question sequence from this point.

**7-pre is confirmed merged** (PR #22, merge commit `0622783`). Full brainstorm → spec → plan → subagent-driven implementation (6 tasks) → final whole-branch review (opus, 0 Critical/Important findings) cycle completed in one session. `lib/auth.ts`'s `requireRole(minRole)` was replaced with `requireRole(allowedRoles: UserRole[])` (admin always implicit); ~105 call sites across the codebase were migrated and audited, correcting (not just porting) the role set per actual semantics — closing 3 real privilege-escalation holes: manager could write production data, manager could write finance config, and brewer/manager could read finance data, all via the old linear-rank comparison's floor/ceiling leakage. Local `main` was fetched + ff-merged to `0622783` and independently re-verified clean (no stray commits, matches `origin/main` exactly); the worktree and its now-fully-merged local branch were removed.

**Spec 7 (Export Settings + Barrel Excise Tax) is unblocked and next up.** Its design doc already exists and is approved (`docs/superpowers/specs/2026-06-21-export-settings-design.md`) — go straight to `Skill(superpowers:writing-plans)` for it, no need to re-brainstorm. Its design explicitly depends on `requireRole` supporting an exact `{brewer, admin}` write-gate (excluding manager), which 7-pre now provides — a fresh session should still spot-check that the spec's described usage matches `lib/auth.ts`'s actual current signature before writing the plan, per this project's standing rule of verifying current file state rather than trusting docs blindly.

**Spec 7 implementation is complete; PR #23 is open, not yet merged** (confirmed via `gh pr view 23`: state OPEN, mergeable, `worktree-export-settings` → `main`). Built via subagent-driven-development in worktree `.claude/worktrees/export-settings` — 10 plan tasks, each individually reviewed clean, plus a final whole-branch review (opus) that caught and fixed two real issues before push:
1. **Critical**: the new GET routes used `requireRole(["viewer"])`, which (per 7-pre's new explicit-allow-list semantics) only admits viewer+admin — NOT a floor/hierarchy — so it 403'd `brewer`, the feature's primary user. Fixed to `requireRole(["viewer","brewer","manager"])` on all three new GET handlers; write handlers correctly stayed at `requireRole(["brewer"])`.
2. **Important**: the new `export_service_mappings` table's unique constraint didn't dedupe NULL-keyed default rows (Postgres treats `NULL` as distinct by default), making the upsert route's `onConflict` silently ineffective for `keg_cleaning`/`forklift`/`bulk_discount` defaults. Fixed to `unique nulls not distinct (...)` before the migration was applied.

Migration `20260624_export_settings.sql` was applied to the live database (`drlsazatrcrdwaihjmex`) and independently verified via REST. `npm run lint` (0 errors) and `npm run build` both clean.

**Standing lesson from this session, applies to all future specs**: `requireRole([...])` is a literal allow-list post-7-pre, not a floor — `requireRole(["viewer"])` reads like "open to all authenticated roles" but actually only admits `viewer` + the always-implicit `admin`. Any route intended to be readable by all roles must spell out every role explicitly (e.g. `["viewer","brewer","manager"]`).

**Spec 7 is confirmed merged** (PR #23, merge commit `fa3c790`, merged 2026-06-22T14:44:18Z). The `app/api/finance/account-mappings/route.ts` GET handler flagged above as a possible repeat of the same bug was checked and **is correct, not a bug**: every Finance GET route (`chart-of-accounts`, `transactions`, `statements`, `account-mappings`, `catalog-with-categories`) uniformly uses `requireRole(["viewer"])`, consistent with 7-pre's documented role semantics that manager/brewer explicitly have no finance access at all (not even read). No auth/role follow-up needed before moving on. Local `main` still needs fetch + ff-merge to pick up `fa3c790` (was 13 commits behind as of this note) — confirm with the user before running, then independently re-verify `git log` shows no stray commits (Lesson #6).

**Spec 6 is confirmed merged** (PR #24, merge commit `8efa8d6`, merged 2026-06-22T22:21:42Z). Full resumed-brainstorm → spec → plan → subagent-driven implementation (9 of 10 plan tasks; Task 10's live Square smoke test was explicitly skipped per user choice, since no live `invoice_required` transactions existed to test against) → final whole-branch review (opus) cycle. The resumed brainstorm corrected several details in the "Spec 6 design progress to resume from" note above before it became stale:
- The "new `Invoice Required` status" language was inaccurate — that status already existed from Spec 2a's original `export_transactions` migration; Spec 6 only consumes it.
- Excise Tax line items are rolled up **per receiving_party across the whole invoice** (one line per party, summed), not per transaction.
- Forklift is one flat line **per invoice** regardless of transaction count; Keg Cleaning's quantity is the count of keg-type transactions that actually got a Packaging Fee line (not all keg-type transactions); Bulk Discount is **not a line item** — it's a Square discount object attached directly to keg-type Packaging Fee lines.
- Net terms (due date) needed its own per-partner override (`contract_brewing_partners.export_net_terms_days`) + global default (`system_settings.export_invoice_due_days`) — not in the original brainstorm note at all, surfaced only once due-date computation was discussed concretely.
- A new `lib/square/export-invoices.ts` module was needed (not a `deposit-invoices.ts` refactor) since that module only supports single-line invoices.
- `syncSquareInvoicesForYear` was extracted from the existing Finance ledger sync route so invoice creation can trigger the same sync directly (DRY), without an HTTP round-trip.

Final review caught and fixed two real issues before push: (1) `createExportInvoice` returned `invoiceUrl` from the pre-publish draft response (always null) instead of the post-publish response — fixed to capture the publish response's `public_url`; (2) the spec's "View Invoice" link for already-invoiced rows was never built in the UI task — added a small `GET /api/production/export/invoice-status` route plus a link in `ExportTransactionsTab.tsx`. Also removed now-dead `channel !== "taproom"` branches in `ExportTab.tsx`'s `ExportsChannelTab` left over from the subtab consolidation.

Migration `20260625_export_invoicing.sql` was applied to the live database (`drlsazatrcrdwaihjmex`) and independently verified via REST. `npm run lint` (0 errors, 0 warnings in any file this branch touched) and `npm run build` both clean. Local `main` was fetched + ff-merged to `8efa8d6` and independently re-verified clean (no stray commits, matches `origin/main` exactly); the worktree and its now-fully-merged local branch were removed.

**No live Square invoice was created during this implementation** — Task 10 (the optional live smoke test) was skipped by user choice, and there were no real `invoice_required` export transactions in the live DB to test against at the time. Recommend a manual end-to-end test (create a real export transaction via Export Bay, then generate a real invoice from it) before relying on this feature with real customers — see Lesson #9, a prior verification step does not carry forward to a new live action.

**All 7 originally-numbered specs on this roadmap (1, 2a, 2b, 2c-1, 2c-2, 7-pre, 7) plus Spec 6 are now merged.** Post-merge cleanup confirmed and executed: 3 stale-but-still-registered git worktrees from earlier specs (`allocation-adjustment-refund`, `export-bay`, `export-settings`) plus their already-merged local branches were removed (`git worktree remove` ×3, `git branch -d` ×3) — all content was already safely in `main` via their respective PRs before removal, verified via `git worktree list` first.

**Spec 8 is now defined and next up**: bring the pre-existing deposit-invoice flow (Commitment → Batch → Allocation Plan → Deposit Invoice → Paid → Locked — implemented before Spec 1, never formally speced) up to **functional parity** with Spec 6's newly-built Export Transaction invoicing flow, so the two systems stay in sync rather than diverging. This absorbs the old deferred "lighter audit pass on deposit-invoice status semantics" item from Spec 1's doc — Spec 8 is that audit, scoped now as a real parity/sync effort. User-specified goals, captured verbatim-in-spirit (not yet brainstormed/confirmed in architectural detail):
- Default/customer-specific Square item mapping for **ingredient deposits**, mirroring the `export_service_mappings` pattern from Spec 7 (default + per-partner override) rather than inventing a parallel mechanism.
- Reuse existing components/routes/database tables where Spec 6/7 already built the right shape, instead of building parallel deposit-only versions. Candidates to evaluate for reuse during brainstorming: `export_service_mappings` (mapping infra), Spec 6's unified invoicing UI patterns (`ExportTransactionsTab.tsx`), `lib/square/export-invoices.ts` (the new multi-line invoice module built for Spec 6 — likely a better reuse target than the older single-line-only `lib/square/deposit-invoices.ts`), and `syncSquareInvoicesForYear`.
A fresh session should brainstorm this from scratch (no existing spec doc) — start by reading `lib/square/deposit-invoices.ts`, the Commitment/Allocation Plan/Deposit Invoice status machine, and Spec 6/7's final merged code side by side to find the concrete gaps before proposing architecture.

There is also one untracked file of unknown origin, `supabase/schema_dump_summarized.csv`, sitting in the repo root since before this handoff — the user has not confirmed whether it's safe to delete. Leave it alone; ask again if it's still there and becomes relevant.

**Spec 8's brainstorm got partway through and was paused mid-session** when, while addressing a user-requested bug-fix bundle in `ExportSettingsPanel.tsx`, a structural blocker surfaced: the Packaging Fee mapping section keys off `packaging_item_id`, but `packaging_items` (`supabase/migrations/20260609_baseline.sql:114-126`) conflates two different kinds of rows — physical containers a customer actually receives (e.g. "12oz Can", "16oz Can", "1/2 Keg") and components that are inputs to assembling a container (e.g. "Aluminum (Silver) Lid", "4-Pack (Black) PakTech", "Blank Tray"). This is a real cold-storage/production data-modeling gap, not an invoicing concern, and the user wants it speced and fully completed **before** Spec 8 resumes, exactly like Spec 7 became a hard prerequisite for Spec 6. This became Spec 10 (the table split) — but a later session's brainstorm (below) inserted Spec 9 as a hard prerequisite to Spec 10 itself.

**Spec 9 brainstormed and speced this session, then substantially reframed mid-brainstorm** (`docs/superpowers/specs/2026-06-23-packaging-variations-foundation-design.md`, not yet user-reviewed/committed as of this note — do that next if resuming mid-session, otherwise proceed straight to `Skill(superpowers:writing-plans)` if the user has already approved it).

The brainstorm initially confirmed the Packaging Fee fragmentation (Spec 8 bug-fix-bundle item 3) is caused purely by `ExportSettingsPanel.tsx:248`'s `PackagingFeeSection` listing all `packaging_items` rows with no type filter (live data: 5 containers + 3 components, no duplicate container variants), and found a second real bug: `demand-calendar/route.ts:73-81` guesses a per-lot packaging item via "default for this type" instead of using the real `packaging_item_id` that `cold_storage_inventory` already stores per lot (Spec 1 added that column after this code was written; the join was never updated). An initial narrow "just split into containers/components tables, no BOM needed" design was approved — **then the user reframed the actual problem**: BOM is disconnected from production planning entirely, and the system needs a first-class, strictly-defined "packaging variation" concept (container + format + specific lid/paktech/tray/label components + optional partner exclusivity) so cold storage can track genuinely non-substitutable shippable SKUs (e.g. white-labeled cans for different partners), not just a loose `variant_label` string. This is a much bigger effort spanning 4 subsystems (schema foundation, Recipes UI, Intake/Commitments+deposits, Brewing/kegging-canning+cold storage/Export) — **decomposed into a sequence per user decision**: Spec 9 is foundation-only (schema + definition UI + Recipes UI, consumed by nothing yet), Spec 10 is Brewing/kegging-canning+cold-storage wiring (also where the demand-calendar bug gets fixed, properly, via the variation model), Spec 11 is Export wiring (also where the Packaging Fee fragmentation bug gets fixed properly, by mapping containers only), and Spec 8 (deposit parity) absorbs Intake/Commitments wiring and should be sequenced with whichever of 10/11 covers that area when picked up.

**Two consolidation decisions made during this reframe, apply them when writing Spec 9's plan**: (1) `packaging_items` itself is **not** split into separate tables — the existing `type` column already fully expresses container-vs-component; only a `CHECK` constraint tightening is needed. (2) `packaging_variations` has **no separate junction table** for its component links — four nullable FK columns (`lid_id`/`paktech_id`/`tray_id`/`label_id`) fully capture it since a variation never uses more than one component per category, mirroring the named slots the existing kegging/canning dialog already uses. Both decisions were explicitly driven by the user flagging this database already has 40+ tables and asking to watch for over-fragmentation — weigh future schema proposals on this roadmap against that same bar before reaching for a new table.

**Spec 8's design-so-far (all individually approved by the user this session, do NOT re-litigate these — only the packaging-fee-mapping bug-3 fix below depends on Spec 9; everything else here is final and ready to write straight into the spec doc once Spec 9 is merged):**

1. **Module merge**: `lib/square/deposit-invoices.ts` and `lib/square/export-invoices.ts` merge into one `lib/square/square-invoices.ts`. The generalized multi-line creator (export's `createExportInvoice`/`InvoiceLineItemDraft[]` shape) becomes the only invoice-creation path; deposits become a 1-line-item call into it. `publishInvoice`/`getInvoiceStatus`/`cancelInvoice`/`reviseInvoice` become shared generic functions used by both flows. `calculateIngredientDeposit` (pure math, no Square calls) stays as-is, just feeds one `InvoiceLineItemDraft` instead of calling a deposit-specific creator. Verified: the export module's line-item builder already always sets `base_price_money` regardless of whether a `catalog_object_id` is attached, so a Square item with no preset price (Ingredient Deposit) paired with our computed price already works natively in the merged shape — no extra work needed there.

2. **Schema**:
   - `export_service_mappings` **renames to `invoice_item_mappings`** (no longer export-specific once it covers deposits too).
   - `invoice_item_mappings.service_type` check-constraint gains `'ingredient_deposit'`, same shape as `keg_cleaning`/`forklift`: `packaging_item_id` always NULL, `square_catalog_item_id` + `square_catalog_variation_id` required, `square_catalog_discount_id` NULL. Keyed by `(service_type, partner_id)` only — default (`partner_id` NULL) + per-partner override, no packaging-item dimension (deposit amount is a single computed dollar figure, not packaging-dependent). Existing `unique nulls not distinct (...)` constraint already supports this pattern unchanged.
   - New `contract_brewing_partners.deposit_net_terms_days` (nullable int, per-partner override) — **distinct column from** `export_net_terms_days`, not reused, since deposit due dates and export due dates are different business concepts even though both are now "days from invoice date."
   - New `system_settings.deposit_invoice_due_days` (int, global default), parallel to `export_invoice_due_days`.
   - `batch_allocations` needs no new columns. `export_transactions.status` already has `'paid'` as a valid value, just never reached — no schema change, only wiring (see #4).

3. **API routes**:
   - `app/api/production/allocations/[id]/invoice/route.ts`: `generate` builds one `InvoiceLineItemDraft`, resolving the Square item via `invoice_item_mappings` (partner override → default) instead of the removed `findIngredientDepositVariationId`/env-var lookup; due date now from `deposit_net_terms_days` → `deposit_invoice_due_days` global default, instead of `batch.expected_delivery_date`. `send` calls the shared `publishInvoice()`. `sync` gains a lighter post-generate "did the draft actually get created" check, in addition to its existing paid-status check.
   - `app/api/production/export/invoice/route.ts`: **stops auto-publishing on creation** (this was a real, accidental gap, not a deliberate design choice — confirmed no code anywhere ever transitions `export_transactions.status` from `'unpaid'` to `'paid'`, and the "Paid" badge in the UI is dead code today). `generate` now only creates the DRAFT. Same route gains `send` and `sync` actions (mirroring deposit's exact action-param shape), addressed via the already-known `square_invoice_id` stored on the export_transactions rows rather than re-deriving the transaction grouping. `sync` flips `export_transactions.status` to `'paid'` when Square confirms payment — closing the dead-badge gap.
   - `findIngredientDepositVariationId` (`lib/square/catalog.ts`) and `SQUARE_INGREDIENT_DEPOSIT_VARIATION_ID` env var are removed as dead code once `invoice_item_mappings` owns this lookup.
   - No cron jobs exist anywhere in this repo (confirmed via search) and Spec 8 does not add any — all sync remains manual-button-triggered; user explicitly deferred cron-based auto-sync to a later, separate effort.

4. **UI**:
   - New top-level `PRODUCTION_NAV` entry: `{ href: "/production/settings", label: "Settings" }` (sibling to Intake/Brewing/Export/Recipes/Inventory/Partners), with its own sub-nav (mirroring `BREWING_NAV`'s pattern) for **Deposit Settings** (`/production/settings/deposits`) and **Export Settings** (`/production/settings/export`).
   - `ExportSettingsPanel.tsx` moves under this new route tree (still `scope="full"` for Export Settings, still mirrored at Finance > Settings > excise-tax with `scope="excise-only"` — just repoint that page's import). It gains an `ingredient_deposit` mapping UI section — but per the user's explicit call, that section's home is actually the **Deposit Settings** sub-tab (grouped with `deposit_net_terms_days`), not Export Settings, even though both are now `invoice_item_mappings` rows.
   - New `DepositSettingsPanel.tsx` (or a new `scope` on `ExportSettingsPanel`) for Deposit Settings: `deposit_net_terms_days` global + per-partner override, plus the `ingredient_deposit` mapping section.
   - `ExportTab.tsx` loses its internal `"settings"` tab from `TOP_TABS` — Export Bay / Taproom / Export Transactions remain.
   - `ExportTransactionsTab.tsx` gains "Send" and "Sync" buttons/states per invoiced-but-unsent / sent-but-unpaid groups, mirroring `BatchLogTab.tsx`'s existing pattern. `BatchLogTab.tsx`/`DepositInvoiceModal.tsx` need no new UI beyond what exists — same buttons, now backed by the merged module + new due-date logic.

5. **Bug-fix bundle, surfaced while reviewing `ExportSettingsPanel.tsx` for the above, all in scope for Spec 8 except #3 which is blocked on Spec 9**:
   1. **Excise Tax Rates can't be edited** — root cause confirmed: the PATCH route (`app/api/production/export-settings/excise-tax-rates/[id]/route.ts:1-41`) works fine, but the UI (`ExportSettingsPanel.tsx` lines 17-65) only has edit controls for `square_catalog_item_id`/`square_catalog_variation_id`/`is_active` — `name`, `receiving_party`, `unit`, `rate_usd` are display-only with no edit form at all. Fix: add edit controls for all four missing fields.
   2. **No mapping section lets you add a partner-specific override** — root cause confirmed: `SimpleServiceSection` (lines 287-322) and `BulkDiscountSection` (lines 324-353) hardcode `partner_id: null` with zero partner-picker UI; `PackagingFeeSection` (lines 206-285) *displays* existing partner overrides but has no control to create a new one. The PUT route (`app/api/production/export-settings/service-mappings/route.ts:32-80`, soon `invoice_item_mappings`) already accepts and upserts non-null `partner_id` correctly — this is purely a missing UI control, not a backend gap. Fix: add a partner-select + "add override" control to every mapping section.
   3. **Packaging Fee mapping fragments by `packaging_item_id` instead of the 5 canonical package types** (1/2 Keg, 1/4 Keg, 1/6 Keg, 12oz Can, 16oz Can) — **this is the Spec 9 blocker**, not a standalone Spec 8 fix. Once Spec 9 resolves the container/component distinction in `packaging_items`, Spec 8's `PackagingFeeSection` should key its mapping rows off whatever canonical container-type signal Spec 9 introduces, not raw `packaging_item_id`.

A fresh session resuming this should: confirm Spec 9 is fully merged first (check this table + `git log`), then re-confirm with the user that sections 1-5 above (Spec 8's actual design) still hold before writing the spec doc — per this project's standing rule of verifying current state, not trusting a stale note, though in this case the note is recent and the design was reached via direct user approval per-section this same session.

**Spec 9 is confirmed merged** (PR #25, merge commit `d875590`, merged 2026-06-23T19:58:10Z). Brainstorming substantially reframed the original "container vs. component table split" idea into a proper `packaging_variations` entity (container + format + 4 nullable component-FK slots + optional partner exclusivity, no junction table — see ROADMAP's earlier note on the two consolidation decisions) and decomposed the effort into Specs 9/10/11 plus folding remaining wiring into Spec 8. Built via subagent-driven-development (7 plan tasks, one fix round on Task 3, final whole-branch review on opus caught a lint error none of the per-task dispatches had checked for — only `npm run build` was run per-task, not `npm run lint` — plus hardened migration seed-insert idempotency). Migration `20260626_packaging_variations.sql` applied to the live database (`drlsazatrcrdwaihjmex`) and independently verified via REST; 11 generic variations seeded. **Lesson for future specs: always include `npm run lint` explicitly in implementer/reviewer dispatch instructions — `npm run build` alone does not run ESLint under Turbopack and will not catch lint errors.** Local `main` was fetched + ff-merged to `d875590` and independently re-verified clean; the worktree (entered via the native `EnterWorktree` tool, not a manual git worktree) was removed via `ExitWorktree`.

Note for whoever picks up bug-fix-bundle item 5.3 above: that fix now belongs to **Spec 11** (Export wiring), not Spec 8 directly, per the Spec 9/10/11 decomposition — `PackagingFeeSection` should key off `packaging_variations.container_id` (or the `packaging_items` container rows directly) once Spec 11 does that wiring.

**Specs 10/11/8 reworked (this session, post-Spec-9-merge) after spotting a logical gap**: Spec 9 built `packaging_variations` as a clean, strictly-defined entity, but every actual consumer it was meant to eventually feed — `transfers/route.ts`, `cold_storage_inventory`, `ExportBayTab.tsx`'s ship/inventory-line picker, `demand-calendar/route.ts` — still runs entirely on the old model: ad-hoc component assembly at transfer time plus a free-text `variant_label` string, with zero structured link to any `packaging_variations` row. The original Spec 10/11 descriptions ("migrate X to consume `variation_id`") understated this — it's not a rekey, it's replacing free-text entry with constrained selection.

**User-confirmed principle going forward: packaging variations, once declared for a recipe via `recipe_packaging_variations`, are the *only* valid choices downstream — no ad-hoc component assembly and no free-text `variant_label` once a flow is migrated.** This reframes Spec 10's `TransferModal.tsx` work from a backend rekey into a real UI change (assemble-and-type → pick-from-declared-list), and extends the same principle to Spec 8's Intake/Commitments packaging-preference piece (`commitment_packaging_preferences` should likewise pick only from a recipe's declared set, not assemble freely) — see updated row descriptions in the table above.

**User-confirmed decision on legacy data**: no backfill/reconciliation of existing free-text `variant_label` rows in `cold_storage_inventory` — live inventory is low-stakes enough to just reset; Spec 10 only needs to support fresh transfers under the new model going forward, not migrate history.

**Spec 11 was reassessed against this same principle and found mostly unaffected**: Packaging Fee mapping is volume-only (keys off `container_id`), independent of which specific variation/components were used, so it doesn't inherit the strict-consumption requirement itself. One open question deferred to Spec 11's own brainstorming: whether `ExportBayTab.tsx`'s ship/inventory-line picker (which also reads/writes free-text `variant_label` against `cold_storage_inventory`) gets fixed for free once Spec 10 rekeys cold storage, or needs its own explicit task — don't assume either way without checking Spec 10's actual final shape.

Sequencing recommendation unchanged: **11 → 10 → resume 8** (11 is the narrowest slice and directly unblocks resuming Spec 8's paused brainstorm; 10 is the one that absorbs the bulk of this rework and should be fully speced/built before Spec 8's Intake/Commitments piece, since both now share the same strict-consumption pattern and Spec 8 should reuse whatever UI/API shape Spec 10 establishes rather than reinventing it).

**Resolved during this session — Packaging Fee mapping's lookup direction, and how to model containers of identical volume that are physically distinct stock**: a `packaging_variations` row's `container_id` is `not null`, so the invoicing-time lookup always goes `export transaction → packaging_variation_id → packaging_variations.container_id → fee mapping row for that container_id`, never the reverse — a container with zero variations defined yet isn't "phantom," it's just unconfigured-but-resolvable once a variation does exist for it. The real risk to design against is the opposite: a container ships via a real variation but has no fee mapping configured — Spec 11 should fail loudly on that (matching this project's established convention, e.g. Spec 2b's export-bay-equipment-missing fix) rather than silently omitting the fee line.

Separately, confirmed the right way to model **two physically distinct containers of the same volume and purpose** (e.g. a house-owned 1/6 Keg vs. a partner-owned 1/6 Keg — same volume, but separate trackable stock): **two separate `packaging_items` rows**, not one row with a `partner_id` override. `packaging_items` is the inventory-tracking catalog (what `cold_storage_inventory` keys off) — if the stock can't be told apart, it can't be tracked separately, regardless of matching volume/purpose. Each gets its own `packaging_variations` row(s) and its own independent Packaging Fee Settings row (two rows, not a default+override pair, since they're not billing exceptions of the same container — they're genuinely different containers that happen to cost the same). `packaging_variations.partner_id` remains a separate, still-useful concept on top of this: it marks a variation as *exclusive* to a partner, independent of which `packaging_items` container it's built from. Use `partner_id`-as-override only when the container truly is the same trackable stock and the difference is purely a billing/fee exception — not whenever two containers happen to share a volume.

**Spec 11 is confirmed merged** (PR #26, merge commit `861ca37`, merged 2026-06-24T01:31:09Z). Investigation during brainstorming found the bug was narrower than originally framed: `export_transactions.packaging_item_id` already only ever holds a container id in practice (cold storage tracks containers only; components are consumed earlier at kegging/canning time, not at ship time), so the invoicing-time lookup path was never actually buggy — the fragmentation lived entirely in `ExportSettingsPanel.tsx`'s Settings UI, which listed every `packaging_items` row (containers and components alike) as fee-mappable. Result: **no schema/migration, no column rename** — `export_service_mappings.packaging_item_id` was kept as-is. Two small fixes only: (1) `PackagingFeeSection` now filters to `type in ('keg','can')` before rendering default mapping rows, mirroring the existing precedent in `PackagingVariationsPanel.tsx:39`; (2) `lib/production/exportInvoicePreview.ts` now throws a descriptive error naming the container instead of silently skipping the Packaging Fee line when no mapping is configured — closing a real latent under-billing risk (confirmed live: zero `packaging_fee` mapping rows exist in the DB today, so every one of the 5 containers would currently hit this path on a real invoice attempt). Built via subagent-driven-development (2 plan tasks, both approved clean on first review; final whole-branch review on opus found only a cosmetic stale-comment nit, fixed directly). `npm run lint` (0 errors) and `npm run build` both clean throughout — no migration to apply or verify against the live DB for this spec. Local `main` was fetched + ff-merged to `861ca37` and independently re-verified clean (no stray commits, matches `origin/main` exactly); the worktree (entered via `EnterWorktree`) was removed via `ExitWorktree` after confirming its commits were already present on `origin/main`.

**Spec 10 is confirmed merged** (PR #27, merge commit `70714c3`, merged after this handoff). Full brainstorm → spec → plan → subagent-driven implementation (8 plan tasks across 11 commits, every task individually reviewed, final whole-branch review on sonnet — 0 Critical/Important findings) cycle completed in one session. Key decisions and findings during this build:
- `packaging_variations` gained a server-computed, trigger-maintained `total_volume_fl_oz` column (computed from `container.volume_fl_oz × unitsPerPackage`, cascade-recomputed via a Postgres `AFTER UPDATE` trigger on `packaging_items` if a container/component's volume or can_count ever changes) — this became the single source of truth for volume math everywhere downstream (transfers, export-bay shipping), replacing per-call joins to derive units-per-package.
- Rather than a new child table for multi-variation transfers (considered and rejected per this project's standing "avoid over-fragmentation" bar — see Spec 9's note above), the **user's own suggestion** was adopted instead: `batch_transfers` gained nullable `variation_id`/`quantity` columns directly, and a single kegging/canning action producing N variations now creates N `batch_transfers` rows (one per variation), verified safe since `volumeLedger.ts`/`commitments.ts` already treat the table as a purely additive ledger with no per-row-as-discrete-action assumption.
- `cold_storage_inventory` was rekeyed from `(batch_id, packaging_item_id, variant_label)` to `(batch_id, variation_id)`; the table was truncated as part of the migration (confirmed disposable test data, no backfill, per the design's stated decision).
- **A real migration bug was caught and fixed mid-build**: Postgres rejects referencing an `UPDATE` target's own alias inside the `FROM`-clause's `JOIN ON` conditions (`UPDATE t AS v SET ... FROM a JOIN b ON b.id = v.col` is invalid) — both the one-time backfill and the cascade trigger function had this exact bug; fixed by moving the joins into a correlated scalar subquery in the `SET` expression. **Lesson for future specs using `UPDATE ... FROM` with multiple joined tables: never reference the UPDATE target's alias inside the FROM-clause's own JOIN conditions — use a correlated subquery in SET instead, and test the exact SQL against the live dashboard before assuming a migration "looks right."**
- **A pre-existing Spec 9 seed-data bug was found and fixed live during Task 1's verification**: 6 of the 11 seeded `packaging_variations` rows (Case/4-Pack/6-Pack format for both can sizes) had `tray_id`/`paktech_id` left `null` despite their `format` requiring one — direct SQL seed inserts had bypassed the route-level `validateFormat()` invariant. There was also no paktech catalog item with `can_count=6` at all. User added the missing catalog item; all 6 variations were corrected via user-approved REST `PATCH` calls with hand-computed `total_volume_fl_oz` values.
- **A real plan gap was found and absorbed mid-build**: `BrewStatusTab.tsx` (4 sites) and `BrewsSubtab.tsx` (1 site) also referenced the now-dropped `kegging_detail`/`canning_detail` fields and were never in the original plan's file list — discovered via `tsc` errors during Task 2's verification, fixed as additional scope folded into Task 5's dispatch.
- `ExportBayTab.tsx` and the export-bay ship routes were rekeyed to `variation_id` as part of this spec (resolving the open question Spec 11 deferred) — `export_transactions`/`ExportServiceMapping` themselves remain untouched, per Spec 11's prior confirmed-correct design (volume-only fee mapping, independent of which variation was used).
- **No live click-through was possible**: confirmed via direct query that zero kegging/canning `batch_transfers` rows exist in the live DB, so the new flow has only been verified by code review, hand-traced arithmetic, and static analysis — not exercised end-to-end through the running UI. Recommend a manual real-transfer test once deployed.

Local `main` was fetched + ff-merged to `70714c3` and independently re-verified clean (log shows exactly the 11 expected commits plus the merge commit, no stray content); the worktree (entered via `EnterWorktree`) was removed via manual `git worktree remove` (the native `ExitWorktree` tool no-op'd, reporting no active session — likely lost across a context compaction earlier in the session) after confirming its branch was fully contained in `main`.

**Sequencing note**: an agent mid-session accidentally ran `git stash -u` in the main repo checkout instead of the worktree (a `cd` slip). It stashed nothing of substance (main was clean except the untracked `.claude/worktrees/` directory marker) and was immediately popped back; main's status was verified byte-identical to before. No data was lost, but flagging the pattern: **always double-check `pwd` before any git command that isn't obviously read-only, especially right after a multi-command bash block that `cd`s between the main repo and a worktree.**

**Spec 8 is confirmed merged** (PR #28, merge commit `d92b56e`). Full brainstorm (re-confirming the prior session's paused design-so-far still held, then a fresh brainstorm for the genuinely new `commitment_packaging_preferences` piece) → spec → plan (11 tasks) → subagent-driven implementation → final whole-branch review (opus, 0 Critical, 1 Important, 2 Minor) cycle completed in one session.

- The deposit-invoice cost-calculation piece was explicitly **descoped mid-brainstorm**: the user clarified packaging cost should not fold into the deposit invoice at all — packaging isn't finalized until later in production and fluctuates too much; packaging-cost-on-invoice is deferred to a future spec that will likely attach it to the **export** invoice instead. `commitment_packaging_preferences`'s rekey ended up being UI/API-only (free-pick `packaging_item_id` → strict recipe-scoped `variation_id`, mirroring Spec 10's `TransferModal.tsx` pattern exactly) — `calculateIngredientDeposit` was never touched.
- `export_service_mappings` renamed to `invoice_item_mappings` (no longer export-specific now that it also covers `ingredient_deposit`); `lib/square/deposit-invoices.ts` + `export-invoices.ts` merged into one `lib/square/square-invoices.ts` with a generic `createInvoice` core shared by both flows.
- The real, confirmed gap — nothing in this codebase ever transitioned `export_transactions.status` to `'paid'` — is now closed: `generate` only creates a Square DRAFT (no more silent auto-publish), `send` is now a separate explicit action, `sync` flips status to `'paid'` once Square confirms payment. Deposit and export invoicing now share one `generate \| send \| sync` action shape end-to-end.
- **Two real bugs caught during per-task review, both traced back to bugs in the plan's own brief text (not implementer error)**: (1) a Supabase/PostgREST `.in("partner_id", [id, null])` filter, used to fetch a default-or-partner-override mapping row — PostgREST's `in.()` never matches `NULL` under standard SQL semantics, so the global default mapping was unreachable; fixed to `.or(\`partner_id.eq.${id},partner_id.is.null\`)`, matching an existing correct precedent already in `lib/production/exportInvoicePreview.ts`. **Lesson for future specs: never use `.in("col", [val, null])` to match a nullable column — PostgREST's `in.()` filter never matches NULL; use `.or("col.eq.val,col.is.null")` instead.** (2) The export invoice route's `send`/`sync` actions trusted `txs[0].square_invoice_id` as authoritative for a whole batch without verifying every transaction in the batch actually shared it — fixed with an explicit consistency check before either Square call.
- **A real schema-migration gap surfaced only when the user tried applying it live**: the `commitment_packaging_preferences` rekey added a `not null` `variation_id` column, but one live row had no way to populate it (no mapping from old `packaging_item_id` to a `packaging_variations` row exists) — `alter column ... set not null` failed with a real not-null violation against live data. Fixed by adding a `truncate table` before the column changes, matching Spec 10's exact precedent for the same situation — **explicit user confirmation was obtained before this destructive live-data step**, per this project's standing rule that live data actions are never executed autonomously.
- **Two real gaps were found only during final whole-branch verification, in files that were never in any task's explicit file list** — both are now a standing lesson: `lib/production/exportInvoicePreview.ts` still queried the old `export_service_mappings` table name post-rename (would have broken `GET /api/production/export/invoice-preview` at runtime), and `lib/env.ts`'s `squareIngredientDepositVariationId` accessor was left behind after its only caller (`findIngredientDepositVariationId`) was deleted. **Lesson: a controller's final whole-branch grep for old names/dead code (not just per-task reviews scoped to each task's own file list) is what actually catches renames/removals that don't propagate to every consumer — this caught two real defects this session alone, after 10 individually-clean task reviews missed both.**
- `npm run lint` (0 errors throughout) and `npm run build` both clean at every step. Two migrations (`20260629_invoice_item_mappings.sql`, `20260630_commitment_packaging_strict.sql`) applied to the live database (`drlsazatrcrdwaihjmex`) and independently verified via REST. Local `main` was fetched + ff-merged to `d92b56e` and independently re-verified clean (no stray commits, matches `origin/main` exactly); the worktree (entered via `EnterWorktree`) was removed via manual `git worktree remove` after `ExitWorktree`'s native tool no-op'd (same pattern Spec 10's session hit — likely lost across the mid-session `ExitWorktree({action:"keep"})` call).
- **No live click-through was possible**: the preview tool's working directory is locked to the main repo root and can't render worktree-only code before merge — this feature has only been verified by per-task code review, lint, build, and direct REST checks against the live DB, not exercised end-to-end through the running UI. Recommend a manual test of deposit/export invoice generate→send→sync and the Commitments packaging picker against real data once deployed.

**All specs on this roadmap (1 through 11, plus 8) are now merged.** No spec is currently queued as next — a fresh session should check with the user for what's next before assuming any further work on this roadmap.

## Process this project follows (every spec, no exceptions)

1. **Brainstorm**: `Skill(superpowers:brainstorming)`. Explore current state (usually via an `Explore` subagent survey — cheap, thorough, avoids burning controller context on raw file reads). Flag decomposition immediately if a request bundles independent subsystems — most specs on this roadmap started as one larger ask that got split (e.g. Spec 2c split into 2c-1/2c-2; the very first feature request became this entire 7-item roadmap). Ask clarifying questions one at a time, multiple-choice where possible. Present architecture in sections, get approval per section.
2. **Write the spec doc** to `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md`, self-review for placeholders/contradictions/scope, commit it, ask the user to review.
3. **Write the plan**: `Skill(superpowers:writing-plans)`. Read every file the plan will touch in full (current line numbers, current schema) — don't trust prior specs' line-number references, code shifts between specs. Every task needs complete code in the plan text, not prose descriptions. Self-review for spec coverage / placeholders / type consistency across tasks. **Commit the plan file immediately after writing it** — this has been forgotten at least 3 times across this project's history; it's cheap to forget and easy to catch by running `git status` right after `Write`.
4. **Set up an isolated worktree**: `EnterWorktree({name: "<topic>"})`. Immediately `cp .env.local` into it (gitignored, doesn't come over), `npm install`, `npm run build` to confirm a clean baseline. **Then check `git log` in the new worktree against `git log` on `main`** — `EnterWorktree` branches from the last *pushed* state of `main`, so any spec/plan commits made locally but not yet pushed won't be there; fast-forward-merge them in before dispatching any implementer.
5. **Execute via `Skill(superpowers:subagent-driven-development)`**: fresh implementer subagent per task (cheap model for pure-transcription tasks with complete code in the brief, standard model for multi-file integration, never omit the model param). Every dispatch must tell the subagent to verify, before every single write and every commit: `pwd` / `git branch --show-current` shows the worktree branch, **and** that any absolute file path it's about to write contains the worktree's full path segment (e.g. `.claude/worktrees/<name>`) — not just a `cd` check. This exact mistake (subagent writes to/commits on the shared main checkout despite believing it's in the worktree) has happened **at least 4 times** across this project, including to the currently-in-progress 2c-2 session. See Lessons Learned #6.
6. **Review every task**: `scripts/review-package BASE HEAD` then a fresh reviewer subagent, model scaled to risk (cheap for mechanical diffs, standard/capable for business logic). Never skip the loop on a finding — dispatch a fix, re-review, repeat until clean. The highest-risk task in this whole roadmap (Spec 2b's Ship endpoint, a `creditedQty` reconciliation bug) took 3 review rounds to actually close — don't rush approval on arithmetic/financial logic.
7. **Final whole-branch review** on the most capable model available, with the merge-base diff package, before presenting finish options.
8. **`Skill(superpowers:finishing-a-development-branch)`**: build/lint check, present the 4 options, execute the user's choice. This project has always chosen "push + create PR," never direct merge.
9. **After the user confirms the PR merged**: fetch + ff-merge `main` locally, `ExitWorktree({action: "remove"})`, then **independently verify** `git log` on main shows no stray commits from implementer mistakes (this has happened — see #6) before considering the spec truly closed.

## Lessons learned (apply to every future spec on this roadmap)

1. **No test runner exists in this repo.** Verification is always `npm run lint` + `npm run build` + per-task code review + (where it matters) a direct REST API check via `curl` against the live Supabase project using `.env.local`'s `SUPABASE_SERVICE_ROLE_KEY`. Never claim "verified" without one of these forms of evidence.
2. **`npx supabase db push` is unreliable in this repo** — there's a pre-existing mismatch between `supabase_migrations.schema_migrations` (tracks ~90 fine-grained historical timestamps) and this repo's squashed/renamed local migration filenames. **Never run `supabase migration repair --status reverted`** — doing this once deleted all 90 tracking rows and required a manual SQL restore. To apply a new migration: paste its SQL directly into the Supabase Dashboard SQL Editor for project `drlsazatrcrdwaihjmex`, then separately run `insert into supabase_migrations.schema_migrations (version) values ('<8-digit-date-prefix-of-the-migration-filename>') on conflict (version) do nothing;` so the CLI at least tracks new migrations going forward. This requires asking the user to paste SQL into the dashboard themselves and confirm — the agent cannot do this step itself.
3. **Two real, pre-existing infra bugs were found and fixed mid-project, unrelated to any spec's own scope**: (a) `supabase/.temp/` (Supabase CLI's local link cache) had been accidentally committed and needed gitignoring/untracking; (b) migration `20260617_status_history_and_exhaustion.sql` had silently never been applied to the live database (the `batch_exhaustion` view didn't exist), discovered only when Spec 2a's migration tried to depend on it. **Lesson: don't assume a migration file in the repo means it was actually applied — verify against the live schema via REST API before depending on it.**
4. **`EnterWorktree` branches from the last *pushed* state of `main`**, not local HEAD. After writing a spec/plan doc and committing it locally, always check whether the new worktree actually has those commits (`git log` comparison) and fast-forward-merge them in if not. This has been missed at least twice, requiring mid-session recovery.
5. **The plan file itself gets forgotten** (written via the `Write` tool but never `git add`/`git commit`'d) — happened repeatedly. Always `git status` immediately after writing a plan doc.
6. **The single most common and costly mistake**: a subagent (implementer or even the controller itself, once) believes it's writing inside an isolated worktree but is actually writing to the shared main checkout — because it `cd`'d correctly but then passed an absolute path to a file-write tool that was missing the worktree's directory segment, or because (as with the current 2c-2 session) it never actually entered a worktree in the first place despite believing it had. **Mitigation that actually works**: every implementer dispatch must explicitly require checking that the FULL ABSOLUTE PATH about to be written contains the worktree segment, not just running `git branch --show-current` (which can pass while the file write itself still targets the wrong directory if the path was constructed wrong). When this mistake is caught, the fix is: `git cherry-pick <the-commit>` into the correct worktree branch, then `git reset --hard <commit-before-the-mistake>` on `main` — **always get explicit user confirmation before the `reset --hard` on main**, even though the same content now safely exists in the worktree branch.
7. **The preview/browser testing tool's working directory is locked to the main repo root** — it can never render code that exists only in an unmerged worktree. True browser E2E verification of worktree-only code isn't possible before merge. State this limitation honestly in PR descriptions and final reviews rather than implying full E2E coverage occurred.
8. **Production deploys are manual** (`vercel deploy --prod` from repo root, per this repo's `CLAUDE.md`) — merging a PR to `main` does **not** auto-deploy. If "I don't see the new feature in the live app" comes up, check deployment freshness (ask when `vercel deploy --prod` was last run) before assuming a code bug.
9. **Live financial/Square actions are never executed autonomously.** Real Ship/export submissions against real cold-storage inventory, and (coming in Spec 2c-2) real Square refunds, always require explicit, fresh opt-in from the user — a prior verification step approving something similar does not carry forward to a new action.
10. **Always independently re-verify subagent self-reports** (build output, "files match the brief," hand-traced arithmetic) via direct `Bash`/`Read` rather than trusting the narrative — this caught a real, three-rounds-deep arithmetic bug in Spec 2b's Ship endpoint (`creditedQty` summing didn't actually equal the requested quantity; required computing every credit's quantity in one flat pass over the array, by index, *before* any batch-grouping happened, since `Map`-grouped traversal order isn't guaranteed to match the flat array's order when one batch contributes multiple non-adjacent entries).
11. **Cost-basis-locked-at-payment-time is the established convention for anything involving a customer's deposit**: once a deposit is paid, the dollar amount tracks against the percentage *at that moment*, never recomputed from current ingredient costs later — this exact principle is core to Spec 2c-2's refund math and should be checked for consistency in Spec 6 (Commitments invoicing) if it ever needs to reference what a customer was originally quoted/charged.
12. **Never use `.in("col", [val, null])` in a Supabase/PostgREST query to match a nullable column** — PostgREST's `in.()` filter compiles to SQL `IN (...)`, which never matches `NULL` under standard three-valued logic, so a "partner override or global default (NULL partner_id)" lookup written this way silently never returns the default row. Use `.or("col.eq.val,col.is.null")` instead — this exact bug was caught in Spec 8's deposit invoice route, where the correct pattern already existed one file away in `lib/production/exportInvoicePreview.ts`.
13. **A controller's own final whole-branch grep (for old names, dead code, removed-function references) catches real gaps that per-task reviews miss**, because per-task reviewers are scoped to that task's own file list and can't know a rename/removal needed to propagate somewhere outside it. Spec 8 had 10 individually-clean task reviews, yet the controller's own final grep still caught two real defects (a stale table name in `exportInvoicePreview.ts`, a dead env accessor in `lib/env.ts`) that no task's file list had ever named. Always run this grep yourself before the final whole-branch review, not just trust that per-task reviews summed to full coverage.

## Architecture map (for orienting a fresh session quickly)

- **Schema** (all in `supabase/migrations/`, applied manually per Lesson #2): `cold_storage_inventory` (recipe×packaging-variant×batch on-hand quantities, Spec 1), `export_transactions` + `export_transaction_taxes` (replaces the old `batch_exports`, Spec 2a), `excise_tax_rates` (user-configurable tax rates, Spec 2a), `batch_allocations` (pre-existing, gaining refund-tracking columns in 2c-2), `commitments` (pre-existing), `brew_batches.status` now includes `complete` (replaces the old, incorrectly-triggered `archived`, Spec 2a).
- **`lib/production/`**: `batchCompletion.ts` (`checkAndCompleteBatch`, Spec 2a), `exciseTax.ts` (`computeExciseTaxBreakdown`, Spec 2a), `commitmentFulfillment.ts` (`checkAndFulfillCommitment`, Spec 2b — gated on batch `status === 'complete'`), `coldStorageDepletion.ts` + `exportBayEquipment.ts` + `exportTransactionWriter.ts` (extracted from Spec 2b's Ship endpoint during Spec 2c-1, shared by both the regular Ship flow and Ad-Hoc Export).
- **API routes**: `app/api/production/export-bay/{ship,ship-adhoc,inventory,active-allocation-check}/route.ts` (Specs 2b/2c-1). `app/api/production/allocations/[id]/route.ts` (pre-existing PATCH, gaining the new `/adjust` sibling endpoint in 2c-2). `app/api/production/allocations/[id]/invoice/route.ts` (pre-existing deposit-invoice generate/send/sync — 2c-2 extends its sync action to capture `square_payment_id`).
- **UI**: `app/production/components/ExportTab.tsx` (the Export section's tab shell — Allocations tab was removed and replaced by Export Bay in Spec 2b), `ExportBayTab.tsx` (the 2-column screen, Specs 2b/2c-1), `BatchLogTab.tsx` (pre-existing batch/allocation list — 2c-2 adds a refund-confirmation modal to its existing inline percentage editor).
- **Square integration**: `lib/square/client.ts` (shared request wrapper), `lib/square/deposit-invoices.ts` (pre-existing, `calculateIngredientDeposit`/`createDepositInvoice`/`publishDepositInvoice`/`reviseDepositInvoice`/`getDepositInvoiceStatus`), `lib/square/refunds.ts` (pre-existing read-only `fetchRefunds`, gaining a `createRefund` write function in 2c-2).

## Handoff prompt template (paste into a fresh session to start the NEXT spec)

Once 2c-2 is confirmed merged and this file's table is updated accordingly, use this template to kick off Spec 6 (or whichever is next):

```
Read /Users/will-liao/Desktop/Coding/Git/tpb-square-reports/docs/superpowers/ROADMAP.md in full before doing anything else — it's the durable handoff doc for a multi-spec feature build. Spec <N> (<name>) is next. Follow the "Process this project follows" section exactly, and apply every item in "Lessons learned" — they're all things that have actually gone wrong on this project before. Start with brainstorming (Skill superpowers:brainstorming) for Spec <N>; the roadmap doc's "Architecture map" section should ground your context-gathering, but verify current file state directly rather than trusting it blindly, since code shifts between specs.
```

## Orchestrator bootstrap (paste into THIS session after a context clear)

```
You are the orchestrator for the multi-spec feature build tracked in /Users/will-liao/Desktop/Coding/Git/tpb-square-reports/docs/superpowers/ROADMAP.md. Read that file in full now. Your job going forward is NOT to implement specs yourself in this session — separate sessions handle brainstorming/planning/implementation per spec. Your job is to: track the roadmap table's status, verify merged PRs actually landed (gh pr view / git log, don't trust the table blindly since it can go stale), update the table when a spec completes, and produce the "Handoff prompt template" filled in for whichever spec is next when the user asks you to kick one off. Before doing anything else, check `git log -5` and `git status` on main to see current real state, and ask the user if anything looks like it conflicts with what the roadmap doc says (e.g. an in-progress spec from another session). Do not run any git write operation without explicit confirmation if there's any ambiguity about concurrent session activity.
```
